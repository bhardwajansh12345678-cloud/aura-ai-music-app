Local CORS proxy for the AuraAI project.

Usage:

1. Install dependencies:

```bash
npm install
```

2. Start proxy:

```bash
npm start
```

3. In your app, request through `http://localhost:3000/proxy?url=<encoded_url>`
